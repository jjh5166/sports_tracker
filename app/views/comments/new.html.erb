<div class="InputBox" id="addcm">
<%= form_for @comment, url:(blog_comments_path) do |f| %>
<%= f.hidden_field :user_id, :value => @userid %>
<%= f.hidden_field :blog_id, :value => params[:blog_id] %>
<%= f.text_area :content, maxlength: 150, :required => true %>
<%= f.submit %>
<% end %>
</div>
